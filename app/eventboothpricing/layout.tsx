export const metadata = {
    title: 'event photography by lana dubkova',
    description: 'nyc event photography | brooklyn & manhattan',
}
  
export default function EventBoothPricingLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return children
}